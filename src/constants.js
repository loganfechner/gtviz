/**
 * Constants for status detection module
 */

// Polling
export const POLL_INTERVAL_MS = 5000;        // Default poll interval for status updates

// Timeouts
export const COMMAND_TIMEOUT_MS = 5000;      // Timeout for quick commands
export const EXEC_TIMEOUT_MS = 10000;        // Timeout for execFileSync operations
