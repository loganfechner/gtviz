# gtviz Configuration
# Copy this file to .env and adjust values as needed

# Main server port (WebSocket + REST API)
PORT=3000

# Path to the Gas Town rig directory to monitor
# RIG_PATH=/path/to/your/rig

# Hook polling interval in milliseconds
POLL_INTERVAL=5000

# Metrics broadcast interval in milliseconds
METRICS_BROADCAST_INTERVAL=5000

# Watch mode polling interval in milliseconds (CLI)
WATCH_INTERVAL=2000

# Status detector polling interval in milliseconds
STATUS_POLL_INTERVAL=5000

# Status API server port (gtviz serve command)
STATUS_API_PORT=3847

# Vite dev server port
VITE_PORT=5173

# API proxy target URL (for Vite dev server)
API_URL=http://localhost:3000

# WebSocket proxy target URL (for Vite dev server)
WS_URL=ws://localhost:3000
